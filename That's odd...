# That's odd.. (input olarak N verilen bir sayı dizisinden çift olanların toplamını bulma)

list=[1,2,3,4,5,6,7,8,9]
sumeven(list)


def sumeven(N):
  c=N[:]
  sum=0
  for i in range(len(N)):
    if c[i]%2==0:
      sum+=c[i]
  print(sum)
